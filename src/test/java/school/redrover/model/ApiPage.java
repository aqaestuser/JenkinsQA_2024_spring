package school.redrover.model;

import org.openqa.selenium.WebDriver;
import school.redrover.model.base.BasePage;

public class ApiPage extends BasePage<ApiPage> {
    public ApiPage(WebDriver driver) {
        super(driver);
    }
}
